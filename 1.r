cardata=mtcars
head(cardata)
tail(cardata)
str(cardata)
summary(cardata)
describe(cardata)
table(cardata$am)
table(cardata$gear)
table(cardata$am,cardata$gear)
hist(cardata$mpg)
boxplot(cardata$mpg,horizonta=TRUE)
plot(density(cardata$mpg))
boxplot(cardata$mpg,cardata$hp,cardata$disp)
barplot(table(cardata$am,cardata$gear))
cardata$transmission=ifelse(cardata$am==0,"Automatic","manual")
table(cardata$transmission)
cardata$mileagetype=ifelse(cardata$mpg>22,"High Mileage",ifelse(cardata$mpg>15,"medium Mileage","Low Mileage"))
table(cardata$mileagetype)
aggregate(cardata$mpg~cardata$transmission,FUN=mean)
aggregate(cardata$mpg~cardata$transmission+cardata$mileagetype,FUN=mean)
t.test(cardata$mpg~cardata$transmission)
summary(aov(cardata$mpg~cardata$mileagetype))
chisq.test(table(cardata$transmission,cardata$mileagetype))
cor(mtcars)
cov(mtcars)
heatmap(scale(mtcars))
cardatareg=lm(mpg~hp+wt+am,data=cardata)
summary(cardatareg)
plot(cardatareg)
cardatarpart=rpart(rpart(mpg~.,data=cardata))
plot(cardatarpart)
text(cardatarpart)
rpart.plot(cardatarpart)
